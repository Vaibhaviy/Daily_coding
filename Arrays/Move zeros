class Solution {
public:
    void moveZeroes(vector<int>& nums) {

    vector<int> temp;

    for(int x : nums){
        if(x != 0)
            temp.push_back(x);
    }

    int zeros = nums.size() - temp.size();

    while(zeros--)
        temp.push_back(0);

    nums = temp;
}
};
