class Solution {
public:
    int maxSubArray(vector<int>& nums) {
    int max_sum=INT_MIN;
    int curr=0;
    for(int val: nums){
        curr+=val;
        max_sum=max(curr,max_sum);
        if(curr<0){
            curr=0;
        }
    } return max_sum;   
    }

};
